version: v1
kind: View
metadata:
  name: some-view
  catalog: test-catalog
  description: Should not fail
spec:
  rules:
    - intent: Allow
      actions:
        - "variant.list"
      targets:
        - res://catalog/test-catalog/variant/test-variant
