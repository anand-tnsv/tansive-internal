version: v1
kind: View
metadata:
  name: some-view
  catalog: test-catalog
  description: Should not fail
spec:
  rules:
    - intent: Allow
      actions:
        - "variant.list"
      targets:
        - res://variants/test-variant
