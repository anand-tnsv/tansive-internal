version: v1
kind: ParameterSchema
metadata:
  name: integer-param-schema
  description: integer-param-schema-description
spec:
  dataType: Integer
  validation:
    minValue: 1
    maxValue: 10
  default: 5
