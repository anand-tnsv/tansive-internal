package apiversion

const (
	// Group is the API group identifier
	Group = "catalog.tansive.io"

	// Version is the API version
	Version = "v1alpha1"

	// FullVersion is the fully qualified API version string
	FullVersion = Group + "/" + Version
)
